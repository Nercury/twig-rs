extern crate twig;

use super::support;

// #[test]
// #[should_panic(
//     expected = r#"Attribute call does not support named arguments"#
// )]
// fn attribute_call_does_not_support_named_arguments() {
//     support::maybe_parsed(r#"{{ foo.bar(name="Foo") }}"#).unwrap();
// }
